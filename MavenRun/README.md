How to use sikulixapi.jar snapshots in Maven projects
-----------------------------------------------------

... just an example (works with Java 1.7, 1.8 and 9)

**Usage:**<br>
being in the folder
  - `mvn clean install`
  - `mvn exec:exec`
