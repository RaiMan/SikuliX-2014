How to use sikulixapi.jar snapshots in Maven projects
-----------------------------------------------------

... just an example

**Usage:**<br>
being in the folder
  - `mvn clean install`
  - `mvn exec:exec`
